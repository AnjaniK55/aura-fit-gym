# VITE_API_URL defines the base URL for the backend API server in production.
# Leave blank to use relative paths (e.g. if client and server are hosted together, or during local dev proxy).
VITE_API_URL=https://your-api-server.com
